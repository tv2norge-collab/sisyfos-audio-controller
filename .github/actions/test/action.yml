name: "Test"
description: "Test"
runs:
  using: "composite"
  steps:
    - uses: ./.github/actions/cache
    - name: Install dependencies
      run: yarn install --mode=skip-build
      shell: bash
    - name: Run tests
      run: yarn test
      shell: bash